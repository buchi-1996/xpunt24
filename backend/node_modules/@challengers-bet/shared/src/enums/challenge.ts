export enum ChallengeStatus {
  OPEN = 'OPEN',
  MATCHED = 'MATCHED',
  LOCKED = 'LOCKED',
  SETTLED = 'SETTLED',
  CANCELLED = 'CANCELLED',
  DISPUTED = 'DISPUTED',
  EXPIRED = 'EXPIRED',
}

export enum ChallengeVisibility {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}
