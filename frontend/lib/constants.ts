// Platform limits — keep in sync with backend env defaults (MIN_STAKE, MIN_DEPOSIT, MIN_WITHDRAWAL, …).
// All amounts are in USDT.
export const MIN_STAKE = 2
export const MIN_DEPOSIT = 10
export const MIN_WITHDRAWAL = 10
export const WITHDRAWAL_REVIEW_THRESHOLD = 1000
